import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.weekend.build',
  appName: 'weekend-build-template',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
